/* Prefixo de asset servido pelo Worker w-lst-paginas.
   Vem do `base` do vite.config, então dev e produção usam o mesmo valor
   e nada aqui precisa saber o slug. */
export const BASE = import.meta.env.BASE_URL;
